version: 3

project:
  locator: SolaceProducts_solace-agent-mesh-helm-quickstart
  id: SolaceProducts_solace-agent-mesh-helm-quickstart
  name: solace-agent-mesh-helm-quickstart
  teams: []
  labels:
    - helm

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
