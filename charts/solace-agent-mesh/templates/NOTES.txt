{{- /* ===== Platform-aware helpers — Go mode renders different access patterns ===== */ -}}
{{- $isGo := eq (.Values.sam.platform | default "python") "go" -}}
{{- $component := ternary "gwe" "core" $isGo -}}
{{- /* ===== Compute config summary variables ===== */ -}}
{{- $hasOidc := and .Values.sam.oauthProvider .Values.sam.oauthProvider.oidc .Values.sam.oauthProvider.oidc.issuer (ne .Values.sam.oauthProvider.oidc.issuer "") -}}
{{- $showWarning := or .Values.global.broker.embedded .Values.global.persistence.enabled (not .Values.sam.authorization.enabled) (and (not .Values.ingress.enabled) (ne .Values.service.type "LoadBalancer")) -}}
{{- /* Config line segments */ -}}
{{- $brokerCfg := ternary "Embedded" "External" .Values.global.broker.embedded -}}
{{- $persistCfg := ternary "Embedded" "External" .Values.global.persistence.enabled -}}
{{- $netCfg := "" -}}
{{- if .Values.ingress.enabled -}}
  {{- $netCfg = "Ingress" -}}
  {{- $ingressTls := include "sam.notes.ingressTls" . -}}
  {{- if eq $ingressTls "true" -}}
    {{- $netCfg = printf "%s + TLS" $netCfg -}}
  {{- end -}}
{{- else if eq .Values.service.type "LoadBalancer" -}}
  {{- $netCfg = "LoadBalancer" -}}
  {{- if .Values.service.tls.enabled -}}
    {{- $netCfg = printf "%s + TLS" $netCfg -}}
  {{- end -}}
{{- else if eq .Values.service.type "NodePort" -}}
  {{- $netCfg = "NodePort" -}}
  {{- if .Values.service.tls.enabled -}}
    {{- $netCfg = printf "%s + TLS" $netCfg -}}
  {{- end -}}
{{- else -}}
  {{- $netCfg = "ClusterIP" -}}
  {{- if .Values.service.tls.enabled -}}
    {{- $netCfg = printf "%s + TLS" $netCfg -}}
  {{- end -}}
{{- end -}}
{{- $authCfg := "" -}}
{{- if $hasOidc -}}
  {{- $authCfg = "OAuth2/OIDC" -}}
{{- else if .Values.sam.authorization.enabled -}}
  {{- $authCfg = "Enabled" -}}
{{- else -}}
  {{- $authCfg = "Disabled" -}}
{{- end -}}

================================================================================
  Solace Agent Mesh (SAM) {{ .Chart.Version }} installed!
================================================================================

  Release:   {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}

  Config:
    Broker:      {{ $brokerCfg }}
    Persistence: {{ $persistCfg }}
    Network:     {{ $netCfg }}
    Auth:        {{ $authCfg }}
{{- if $showWarning }}

  This config will get you started, but it is not ready for production use.
  See: https://solaceproducts.github.io/solace-agent-mesh-helm-quickstart/docs/
{{- else }}

  ✅  Configured for production use.
{{- end }}

================================================================================
  GETTING STARTED
================================================================================
{{- if .Values.ingress.enabled }}
{{- $externalHost := include "sam.notes.ingressHost" . }}
{{- $scheme := include "sam.notes.ingressScheme" . }}
{{- if $externalHost }}

  Console: {{ $scheme }}://{{ $externalHost }}
{{- else }}

  Console: <ALB DNS will be assigned>
    kubectl get ingress {{ include "sam.names.fullname" . }} -n {{ .Release.Namespace }}
{{- end }}
{{- if and (not .Values.ingress.autoConfigurePaths) (not $isGo) }}
{{- $hasPlatformPath := false }}
{{- range .Values.ingress.hosts }}
  {{- range .paths }}
    {{- if hasPrefix "/api/v1/platform" .path }}
      {{- $hasPlatformPath = true }}
    {{- end }}
  {{- end }}
{{- end }}
{{- if not $hasPlatformPath }}

  ⚠️  WARNING: Platform service path (/api/v1/platform) not found in ingress configuration!
     Platform APIs will not be accessible.

     Either:
     1. Set ingress.autoConfigurePaths: true (recommended)
     2. Add the following path to ingress.hosts[].paths (before the catch-all / path):

        - path: /api/v1/platform
          pathType: Prefix
          portName: platform
{{- end }}
{{- end }}
{{- else if eq .Values.service.type "LoadBalancer" }}

  Get external IP (Ctrl-C when assigned):
    kubectl get svc {{ include "sam.names.component" (dict "root" . "component" $component) }} -n {{ .Release.Namespace }} -w
{{- if .Values.service.tls.enabled }}

  Open https://<EXTERNAL-IP>
{{- else }}

  Open http://<EXTERNAL-IP>
{{- end }}
{{- else if eq .Values.service.type "NodePort" }}

  Get access URL:
    NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type=="ExternalIP")].address}')
    if [ -z "$NODE_IP" ]; then
      NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type=="InternalIP")].address}')
    fi
{{- if .Values.service.tls.enabled }}
    PORT=$(kubectl get svc {{ include "sam.names.component" (dict "root" . "component" $component) }} -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[?(@.name=="webui-tls")].nodePort}')

  Open https://$NODE_IP:$PORT
{{- else }}
    PORT=$(kubectl get svc {{ include "sam.names.component" (dict "root" . "component" $component) }} -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[?(@.name=="webui")].nodePort}')

  Open http://$NODE_IP:$PORT
{{- end }}
{{- else }}

  Wait for pods to be ready:
    kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} -w

  Port-forward the Console UI:
{{- if .Values.service.tls.enabled }}
{{- if $isGo }}
    kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "sam.names.component" (dict "root" . "component" $component) }} 8443:443

  Then access the Console at:
    https://localhost:8443
{{- else }}
    kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "sam.names.component" (dict "root" . "component" $component) }} 8443:443 4443:4443

  Then access the Console at:
    https://localhost:8443
{{- end }}
{{- else }}
{{- if $isGo }}
    kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "sam.names.component" (dict "root" . "component" $component) }} 8080:80

  Then access the Console at:
    http://localhost:8080
{{- else }}
    kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "sam.names.component" (dict "root" . "component" $component) }} 8000:80 8080:8080

  Then access the Console at:
    http://localhost:8000
{{- end }}
{{- end }}
{{- end }}
{{- if $hasOidc }}

  On first login you will be redirected to your identity provider.
{{- if .Values.ingress.enabled }}
{{- $callbackHost := include "sam.notes.ingressHost" . }}
{{- $callbackScheme := include "sam.notes.ingressScheme" . }}
{{- if $callbackHost }}

  Ensure your OIDC callback URI is registered:
    {{ $callbackScheme }}://{{ $callbackHost }}/api/v1/auth/callback
{{- else }}

  Ensure your OIDC callback URI is registered:
    {{ $callbackScheme }}://<INGRESS-HOST>/api/v1/auth/callback
  If the ingress host is assigned later, retrieve it with:
    kubectl get ingress -n {{ .Release.Namespace }}
{{- end }}
{{- else if and .Values.sam.dnsName (ne .Values.sam.dnsName "") }}
{{- $callbackScheme := ternary "https" "http" .Values.service.tls.enabled }}

  Ensure your OIDC callback URI is registered:
    {{ $callbackScheme }}://{{ .Values.sam.dnsName }}/api/v1/auth/callback
{{- end }}
{{- end }}
{{- if not (and .Values.llmService .Values.llmService.llmServiceApiKey (ne .Values.llmService.llmServiceApiKey "")) }}

  On first login you'll be prompted to configure your LLM API key.
{{- end }}

  If something looks wrong:
    kubectl get pods -n {{ .Release.Namespace }}
    kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} --tail 200
{{- if .Values.ingress.enabled }}
{{- $tsHost := include "sam.notes.ingressHost" . }}
{{- $tsScheme := include "sam.notes.ingressScheme" . }}
{{- if $tsHost }}
    curl -s {{ $tsScheme }}://{{ $tsHost }}/health
    curl -s {{ $tsScheme }}://{{ $tsHost }}/api/v1/platform/health
{{- else }}
    curl -s {{ $tsScheme }}://<INGRESS-HOST>/health
    curl -s {{ $tsScheme }}://<INGRESS-HOST>/api/v1/platform/health
    kubectl get ingress -n {{ .Release.Namespace }}
{{- end }}
{{- else if eq .Values.service.type "ClusterIP" }}
{{- if .Values.service.tls.enabled }}
{{- if $isGo }}
    curl -sk https://localhost:8443/health
    curl -sk https://localhost:8443/api/v1/platform/health
{{- else }}
    curl -sk https://localhost:8443/health
    curl -sk https://localhost:4443/api/v1/platform/health
{{- end }}
{{- else }}
{{- if $isGo }}
    curl -s http://localhost:8080/health
    curl -s http://localhost:8080/api/v1/platform/health
{{- else }}
    curl -s http://localhost:8000/health
    curl -s http://localhost:8080/api/v1/platform/health
{{- end }}
{{- end }}
{{- else if eq .Values.service.type "NodePort" }}
    NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type=="ExternalIP")].address}')
    if [ -z "$NODE_IP" ]; then
      NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type=="InternalIP")].address}')
    fi
{{- if .Values.service.tls.enabled }}
    PORT=$(kubectl get svc {{ include "sam.names.component" (dict "root" . "component" $component) }} -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[?(@.name=="webui-tls")].nodePort}')
    PLATFORM_PORT=$(kubectl get svc {{ include "sam.names.component" (dict "root" . "component" $component) }} -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[?(@.name=="platform-tls")].nodePort}')
    curl -sk https://$NODE_IP:$PORT/health
    curl -sk https://$NODE_IP:$PLATFORM_PORT/api/v1/platform/health
{{- else }}
    PORT=$(kubectl get svc {{ include "sam.names.component" (dict "root" . "component" $component) }} -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[?(@.name=="webui")].nodePort}')
    PLATFORM_PORT=$(kubectl get svc {{ include "sam.names.component" (dict "root" . "component" $component) }} -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[?(@.name=="platform")].nodePort}')
    curl -s http://$NODE_IP:$PORT/health
    curl -s http://$NODE_IP:$PLATFORM_PORT/api/v1/platform/health
{{- end }}
{{- else }}
{{- if .Values.service.tls.enabled }}
    curl -s https://<EXTERNAL-IP>/health
    curl -s https://<EXTERNAL-IP>:4443/api/v1/platform/health
{{- else }}
    curl -s http://<EXTERNAL-IP>/health
    curl -s http://<EXTERNAL-IP>:8080/api/v1/platform/health
{{- end }}
{{- end }}

  Quickstart: https://solaceproducts.github.io/solace-agent-mesh-helm-quickstart/docs/
  Docs:       https://docs.solace.com/Agentic-AI/agent-mesh.htm
