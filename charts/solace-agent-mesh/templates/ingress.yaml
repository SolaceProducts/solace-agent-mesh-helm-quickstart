{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "sam.names.fullname" . }}
  labels:
    {{- include "sam.labels.standard" (dict "root" . "config" (dict "component" "core")) | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- if .Values.ingress.autoConfigurePaths }}
    {{- /* Auto-configured paths mode (recommended) */ -}}
    {{- $host := .Values.ingress.host }}
    - {{- if $host }}
      host: {{ $host | quote }}
      {{- end }}
      http:
        paths:
          {{- $isGo := eq (.Values.sam.platform | default "python") "go" }}
          {{- if not $isGo }}
          - path: /login
            pathType: Prefix
            backend:
              service:
                name: {{ include "sam.names.component" (dict "root" . "component" "core") }}
                port:
                  name: auth
          - path: /callback
            pathType: Prefix
            backend:
              service:
                name: {{ include "sam.names.component" (dict "root" . "component" "core") }}
                port:
                  name: auth
          - path: /is_token_valid
            pathType: Prefix
            backend:
              service:
                name: {{ include "sam.names.component" (dict "root" . "component" "core") }}
                port:
                  name: auth
          - path: /user_info
            pathType: Prefix
            backend:
              service:
                name: {{ include "sam.names.component" (dict "root" . "component" "core") }}
                port:
                  name: auth
          - path: /refresh_token
            pathType: Prefix
            backend:
              service:
                name: {{ include "sam.names.component" (dict "root" . "component" "core") }}
                port:
                  name: auth
          - path: /exchange-code
            pathType: Prefix
            backend:
              service:
                name: {{ include "sam.names.component" (dict "root" . "component" "core") }}
                port:
                  name: auth
          - path: /api/v1/platform
            pathType: Prefix
            backend:
              service:
                name: {{ include "sam.names.component" (dict "root" . "component" "core") }}
                port:
                  name: platform
          {{- end }}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "sam.names.component" (dict "root" . "component" (ternary "gwe" "core" $isGo)) }}
                port:
                  name: webui
    {{- else }}
    {{- /* Manual hosts/paths configuration mode */ -}}
    {{- $manualComponent := ternary "gwe" "core" (eq ($.Values.sam.platform | default "python") "go") }}
    {{- range .Values.ingress.hosts }}
    - {{- if .host }}
      host: {{ .host | quote }}
      {{- end }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "sam.names.component" (dict "root" $ "component" $manualComponent) }}
                port:
                  {{- if .portName }}
                  name: {{ .portName }}
                  {{- else }}
                  name: webui
                  {{- end }}
          {{- end }}
    {{- end }}
    {{- end }}
{{- end }}
