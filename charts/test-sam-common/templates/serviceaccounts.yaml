{{/*
Generate ServiceAccounts for all deployments
Each component gets its own ServiceAccount named: {release-name}-{component}-sa
*/}}
{{- range $component, $config := .Values.deployments }}
---
{{ include "sam.serviceAccount" (dict "root" $ "config" $config) }}
{{- end }}
